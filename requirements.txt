# MANU///LOGS — dependencias
#
# Versiones fijas a proposito: son las que estan probadas en la maquina de
# Manuel (Python 3.13) y las que se van a instalar en el host. Un "pip install
# flask" suelto ya rompio un arranque, y sin este archivo no hay deploy.
#
#     pip install -r requirements.txt

# --- Backend ---
Flask==3.1.3
flask-cors==6.0.2
python-dotenv==1.2.2
SQLAlchemy==2.0.48

# Driver de Postgres (Supabase). psycopg 3, no psycopg2: es el que tiene
# wheels para 3.13. La URL se reescribe sola a postgresql+psycopg:// en
# models.py, asi que la connection string de Supabase se pega tal cual.
psycopg[binary]==3.3.4

# --- Fuentes de datos ---
garminconnect==0.2.40
garth==0.6.3
pdfplumber==0.11.9

# --- Telegram + parser ---
requests==2.32.5
anthropic==0.120.2

# --- Google Sheets (fallback legacy de running y antropometria) ---
# Solo hacen falta si usas ese fallback; el dashboard arranca sin ellas.
google-api-python-client==2.193.0
google-auth==2.49.1
google-auth-httplib2==0.3.0
google-auth-oauthlib==1.3.0
